@model HotelResortMS.Web.Models.Sales.DiscountEditViewModel
@{
    ViewData["Title"] = "Add Discount";
}
<h3 class="mb-3">Add Discount</h3>
<div asp-validation-summary="All" class="text-danger"></div>
<form asp-action="Create" method="post" class="bg-white p-3 rounded border" style="max-width:560px">
    @await Html.PartialAsync("_Form", Model)
    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
</form>
